###### arch linux kill process pid

## Kill a process

*Normal soft kill* = `kill -15 <pid>` oder `kill -SIGTERM <pid>` oder `kill <pid>` 


*Strong force kill* = `kill -9 <pid>` oder `kill -SIGKILL <pid>`
