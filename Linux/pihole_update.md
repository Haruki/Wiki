###### pihole rasbperry pi adguard dns dhcp update

## PiHole update

SSH into raspberrypi (192.168.0.3)

```
pihole -up
```

Achtung: in der Datei /etc/resolve.conf sollte ein gueltiger DNS server stehen.